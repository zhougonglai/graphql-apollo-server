{
  "name": "mongodb-api",
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@now/node-server",
      "config": { "maxLambdaSize": "20mb" }
    }
  ],
  "routes": [
    { "src": "/.*", "dest": "src/index.js" }
  ],
  "build": {
    "env": {
      "NODE_ENV": "production",
      "PORT": "@port"
    }
  },
  "env": {
    "MONGODB_URI": "@mongodb-uri"
  }
}
